name: CI
on:
  workflow_dispatch:
  push:
  pull_request:
#  release:
#    types: [published]
#  schedule:
#    # Weekly tests run on main by default:
#    #   Scheduled workflows run on the latest commit on the default or base branch.
#    #   (from https://help.github.com/en/actions/reference/events-that-trigger-workflows#scheduled-events-schedule)
#    - cron: '0 0 * * *'

jobs:

  changed_files:
    runs-on: ubuntu-latest
    name: 'Get changed files'
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: Get all test, doc and src files that have changed
        id: changed
        uses: tj-actions/changed-files@v37
        with:
          files_yaml: |
            
            docs:
              - 'docs/website/source/**'
              - 'src/**/*.py'
              - 'meta/metadata/*.yaml'
            
            package:
              - 'src/**'
              - 'tests/**/*.py'
              - 'pyproject.toml'
              - 'setup.py'
            

      - name: Verify the contents of the .github/outputs/added_files.json file
        run: |
          echo "${{ steps.changed.outputs.all_changed_and_modified_files }}"
          echo "${{ steps.changed.outputs.docs_any_changed }}"
          echo "${{ steps.changed.outputs.package_any_changed }}"

      - name: 'Create summary'
        run: |
          echo "#### 🏁 Results" >> $GITHUB_STEP_SUMMARY
          echo "- **Docs**: ${{ (steps.changed.outputs.docs_any_changed == 'true' && '✅' || '❌' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Package**: ${{ (steps.changed.outputs.package_any_changed == 'true' && '✅' || '❌' }}" >> $GITHUB_STEP_SUMMARY
          echo "#### 📝 Changed files" >> $GITHUB_STEP_SUMMARY
          for file in ${{ steps.changed.outputs.all_changed_and_modified_files }}; do
            echo "- `$file`" >> $GITHUB_STEP_SUMMARY
          done