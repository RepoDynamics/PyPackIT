# Run static code analysis

name: "Dispatchable: Load Metadata"
run-name: Load repository metadata from ${{ github.head_ref || github.ref }}

on:
  workflow_dispatch:
  workflow_call:
    outputs:
      json:
        description: 'Repository Metadata'
        value: ${{jobs.load.outputs.metadata}}


jobs:

  load:
    name: 'Load'
    outputs:
      metadata: ${{ steps.get-metadata.outputs.json }}
    runs-on: ubuntu-latest
    steps:

      - name: 'Checkout repository from ${{ github.head_ref || github.ref }}'
        uses: actions/checkout@v3
        with:
          # For pull requests, check out the source branch (head_ref), not the target branch (base_ref).
          ref: ${{ github.head_ref }}

      - name: 'Setup environment'
        # https://github.com/mamba-org/setup-micromamba
        uses: mamba-org/setup-micromamba@v1
        with:
          environment-file: repo/requirements.yaml
          cache-environment: true
          cache-environment-key: repo-dynamics
          generate-run-shell: true
          init-shell: bash

      - name: 'Get metadata'
        id: get-metadata
        shell: bash -el {0}
        run: echo "json=$(python repo/dynamics/metadata.py)" >> $GITHUB_OUTPUT

      - name: 'Display metadata'
        if: always()
        run: cat repo/metadata/metadata_full.json

      - name: 'Upload metadata as artifact'
        if: always()
        uses: actions/upload-artifact@v3
        with:
          path: repo/metadata/metadata_full.json
          name: 'metadata'

      - name: 'Create job summary'
        if: always()
        run: |
          echo "Repository metadata loaded from `${{ github.head_ref || github.ref }}/repo/dynamics/metadata.py`. " >> $GITHUB_STEP_SUMMARY
          echo "The JSON output is uploaded as an artifact named `metadata`, and can also be viewed below:" >> $GITHUB_STEP_SUMMARY
          echo "<details>" >> $GITHUB_STEP_SUMMARY
          echo "<summary>🖥 JSON Output</summary>" >> $GITHUB_STEP_SUMMARY
          echo "<pre>" >> $GITHUB_STEP_SUMMARY
          echo "<code>" >> $GITHUB_STEP_SUMMARY
          cat repo/metadata/metadata_full.json >> "$GITHUB_STEP_SUMMARY"
          echo "</code>" >> $GITHUB_STEP_SUMMARY
          echo "</pre>" >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY
