# Repodynamics Base Configurations

You can also create a `config.yaml` file in the local directory to overwrite these configurations when
working on your local device.

Example:
```yaml
repodynamics:
  cache_retention_days:
    extensions: 1
    api: 1
```
